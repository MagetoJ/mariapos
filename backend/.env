# Django Backend Environment Variables
# Copy this file to your Django project root and rename to .env

# Django Secret Key (Generated for immediate security)
SECRET_KEY=django-insecure-8d$d4j%k9h-@sB2aG5cE0fI3uM7zL1oQ6vYpX8tW7rU5eN0p

# Debug Mode (Set to False in production)
DEBUG=True

# Allowed Hosts (FIXED: Added 127.0.0.1 and removed protocol/port)
ALLOWED_HOSTS=localhost,127.0.0.1

# CORS Settings (for frontend communication)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# Database Configuration (Using SQLite for development)
# DATABASE_URL=postgresql://username:password@localhost:5432/mariahavens_pos
# Using SQLite for development:
DATABASE_URL=sqlite:///db.sqlite3

# Database Settings (if not using DATABASE_URL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=mariahavens_pos
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# JWT Authentication
JWT_SECRET_KEY=g(,br]l8El+B'KpPn2*[($"roH<R}`zCZm!N\KBUQP:OepY}Fl
JWT_ACCESS_TOKEN_LIFETIME=3600  # 1 hour in seconds
JWT_REFRESH_TOKEN_LIFETIME=86400  # 24 hours in seconds

# Redis Configuration (for caching and sessions)
REDIS_URL=redis://localhost:6379/0

# Email Configuration (for notifications, password resets, etc.)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Media and Static Files
MEDIA_URL=/media/
STATIC_URL=/static/

# File Upload Settings
MAX_UPLOAD_SIZE=5242880  # 5MB
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf

# API Rate Limiting
API_THROTTLE_RATE_ANON=100/hour
API_THROTTLE_RATE_USER=1000/hour

# Logging Level
LOG_LEVEL=INFO

# Time Zone
TIME_ZONE=Africa/Nairobi

# Language
LANGUAGE_CODE=en-us

# Security Settings (Enable in production)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True

# Payment Integration (if using)
# MPESA_CONSUMER_KEY=your-mpesa-consumer-key
# MPESA_CONSUMER_SECRET=your-mpesa-consumer-secret
# MPESA_SHORTCODE=your-shortcode
# MPESA_PASSKEY=your-passkey

# Third-party Services
# SENTRY_DSN=your-sentry-dsn-for-error-tracking
# CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name